# ---------- Delta Lake ----------
spark.sql.extensions                         io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog              org.apache.spark.sql.delta.catalog.DeltaCatalog

# ---------- S3A -> Silo (object storage) ----------
# Credenciais vêm das variáveis AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY
spark.hadoop.fs.s3a.impl                     org.apache.hadoop.fs.s3a.S3AFileSystem
spark.hadoop.fs.s3a.endpoint                 http://silo:9000
spark.hadoop.fs.s3a.endpoint.region          us-east-1
spark.hadoop.fs.s3a.path.style.access        true
spark.hadoop.fs.s3a.connection.ssl.enabled   false
spark.hadoop.fs.s3a.fast.upload              true

# ---------- Logs das aplicações: apenas avisos e erros ----------
# (master e workers continuam em INFO; os logs vão para o Loki)
spark.log.level                              WARN

# ---------- Ajustes para um cluster local pequeno ----------
spark.sql.shuffle.partitions                 8
spark.sql.adaptive.enabled                   true
spark.serializer                             org.apache.spark.serializer.KryoSerializer

# ---------- Observabilidade (Prometheus, fase 7) ----------
spark.ui.prometheus.enabled                  true
